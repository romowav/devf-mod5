const diaLaborable = ['Lunes', 'Martes', 'Miercoles', 'Jueves', 'Viernes']

const dia = 'Lunes'

const actividad = (diaLaborable.includes(dia)) ? "Nos sabemos la de chambear" : "Descansamos"

console.log(actividad);