const persona = {
  nombre: "cinthya",
  edad: 19,
  ciudad: "CDMX",
};
// Desestructuracion

let { nombre, edad, ciudad } = persona;
// los objetos al tener claves en sus valores, podemos llamar estos datos con variables llamadas igual que las claves, el orden no importa a diferencia qeu los arreglos
console.log(ciudad);
